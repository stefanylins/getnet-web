import Cypress from "cypress";
import { AllureStep, AllureTest } from "allure-js-commons/new";
import { AllureNodeRuntime } from "allure-js-commons/new/node";
export type AllureCypressConfig = {
    resultsDir?: string;
    links?: {
        type: string;
        urlTemplate: string;
    }[];
};
export declare class AllureCypress {
    private config?;
    runtime: AllureNodeRuntime;
    currentTestsByAbsolutePath: Map<string, [AllureTest, number][]>;
    currentSteps: AllureStep[];
    constructor(config?: AllureCypressConfig | undefined);
    private startAllureTest;
    endSpec(spec: Cypress.Spec, results: CypressCommandLine.RunResult): void;
    attachToCypress(on: Cypress.PluginEvents): void;
}
export declare const allureCypress: (on: Cypress.PluginEvents, allureConfig?: AllureCypressConfig) => AllureCypress;
